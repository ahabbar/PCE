# Pin Python 3.11 so wheels resolve. Let Nixpacks auto-handle venv + pip install
# from requirements.txt (puts uvicorn/streamlit on PATH).
[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.build]
cmds = ["mkdir -p data logs"]

[start]
cmd = "uvicorn src.api.server:app --host :: --port $PORT"
